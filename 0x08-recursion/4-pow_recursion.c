#include "main.h"
/**
 * _pow_recursion - the power of anumber
 *
 * @x: number
 * @y: power
 *
 * Return: the result of power
 */
int _pow_recursion (int x, int y)
{
	if (y < 0)
	{
		return (-1);
	}
	else if (y == 0 )
	{
		return (1);
	}
}
 
