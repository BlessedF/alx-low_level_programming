#include "main.h"
/**
 * factorial - grt the factorial of a number
 *
 * @n: factor to factorialise
 *
 * Return: factorial of n
 */
int factorial(int n)
{
	if (n == 0)
	{
		return(1);
	}
	return (n);
}
