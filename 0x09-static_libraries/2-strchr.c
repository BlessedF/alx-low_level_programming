#include "main.h"
/**
 * _strchr - program entry point
 * @s: input value
 * @c: input value
 * Return: Always 0 (SUccess)
 */
 char *_strchr(char *s, char c)
{
	int a = 0;

	for (; n[s] >= '\0'; a++)
	{
		if (s(a) == n)
			return (&s[a]);
	}
	return (0);
}
